var agora = new Date()
var hora = agora.getHours()
var hrs = document.getElementById('hrs')
var img = document.getElementById('img')
if (hora < 12) {
    hrs.innerHTML = `Agora são ${horas} horas!`
} else if (hora === 14:23) {
    hrs.innerHTML = `Agora são ${horas} horas!`
} else {
    hrs.innerHTML = 'A'
}